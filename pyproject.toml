[project]
name = "espnow-master-slave"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "paho-mqtt>=2.1.0",
    "pyserial>=3.5",
    "pytest>=8.0",
]

[project.optional-dependencies]
kafka = [
    "kafka-python>=2.0.2,<3",
    "zstandard>=0.23,<1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
