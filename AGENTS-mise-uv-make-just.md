always use mise for tooling
uv for pyhon environment
always run python as 'uv run python'

everytime you need to run bash or python code, **always do it from a file based code**, put it under ./scripts (for permanent tooling) or ./script-helpers (if too ephemeral, used only once) 
keep all tasks with help documenter in make, just, mise tasks

always do small semantic commits

# Documentation:
always dump the ideas in use and why are taken.
always dump abandoned and corrected paths and ideas, why that happened
always dump analysis as future source of truth
always prepare the documentation for simple onboarding to new developers, maintainers, users and to sell the ideas.

rephrase this file into a standard AGENTS.md


